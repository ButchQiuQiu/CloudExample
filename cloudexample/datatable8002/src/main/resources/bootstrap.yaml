spring:
  cloud:
    config:
      name: datatable
      discovery:
        service-id: config-server
        enabled: true
eureka:
  client:
    service-url:
      #服务注册地址,对应eureka的服务注册url
      defaultZone: http://${EUREKA-IP:127.0.0.1}:7001/eureka/