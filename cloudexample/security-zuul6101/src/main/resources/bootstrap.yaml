eureka:
  client:
    service-url:
      #服务注册地址,对应eureka的服务注册url
      defaultZone: http://localhost:7001/eureka/
spring:
  cloud:
    config:
      name: security-zuul
      discovery:
        service-id: config-server
        enabled: true