server:
  port: 7001
#Eureka配置
eureka:
  client:
    #表示是否向注册中心注册自己
    register-with-eureka: false
    #false代表自己是注册中心
    fetch-registry: false
    service-url:
      defaultZone: http://localhost:7001/eureka/